meta {
  name: Models - Create (FK brand)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/models
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "brand_id": "{{brandId}}",
    "name": "Latitude 5420"
  }
}
