openapi: 3.1.0
info:
  title: Inventra API
  version: 0.1.0
  description: |
    Asset management API for Inventra (see docs/PRD.md, docs/DATABASE.md).

    **Auth:** Bearer JWT. Obtain tokens via `POST /api/v1/auth/login`, then send
    `Authorization: Bearer <access_token>`. Access tokens are short-lived;
    rotate with `POST /api/v1/auth/refresh`. `POST /api/v1/auth/logout` revokes
    the current access token and the supplied refresh token.
  contact:
    name: Inventra
    url: https://github.com/ragbuaj/inventra
  license:
    name: MIT

servers:
  - url: http://localhost:8080
    description: Local development

tags:
  - name: Health
    description: Liveness & readiness probes
  - name: Auth
    description: Authentication & session
  - name: Users
    description: User management (requires the user.manage permission)
  - name: Authorization Admin
    description: |
      Superadmin management of the configurable authorization layer: roles,
      role-permissions, data-scope policies, and field-permissions.
      Role CRUD requires `role.manage`; scope management requires `scope.manage`;
      field-permission management requires `fieldperm.manage`. System roles
      (`is_system: true`) cannot be updated or deleted (returns 409).
  - name: Master Data
    description: |
      Reference master data. Read is open to authenticated users; writes require
      `masterdata.global.manage`. `categories` (documented in full) and `brands`
      (representative of the generic reference CRUD) are shown here.

      The following reference resources share the **same generic CRUD contract**
      as `brands` (`GET/POST /api/v1/{resource}`, `GET/PUT/DELETE /api/v1/{resource}/{id}`),
      differing only in their writable fields:
      `office-types` (field `tier`: `pusat|wilayah|office|null`), `departments`, `positions`,
      `units`, `maintenance-categories`, `problem-categories`, `brands`, `vendors`, `provinces`,
      `cities` (field `province_id`), `models` (field `brand_id`).

      **Offices & employees** (documented in full) are data-scoped per row.
      **Floors** (`GET /api/v1/floors?office_id=...`) and **rooms**
      (`GET /api/v1/rooms?floor_id=...`) are nested, office-scoped location
      resources (writes require `masterdata.office.manage`); see the Bruno
      collection for examples.
  - name: Assets
    description: |
      Asset core. Reads require the `asset.view` permission; writes require `asset.manage`.
      Sensitive financial fields are subject to per-role field-permission masking.
  - name: Requests
    description: |
      Approval requests (maker-checker workflow). Submitting requires `request.create`;
      inbox/decide actions require `request.decide`; listing is open to any authenticated user.
  - name: Approval Thresholds
    description: |
      Value-tiered approval-threshold configuration. All operations require
      the `approval.config.manage` permission.
  - name: Audit
    description: Read-only audit trail (requires the `audit.view` permission)

paths:
  /health:
    get:
      tags: [Health]
      operationId: healthLiveness
      summary: Liveness probe
      description: Returns ok if the process is up. No external dependencies.
      security: []
      responses:
        "200":
          description: Service is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"

  /health/ready:
    get:
      tags: [Health]
      operationId: healthReadiness
      summary: Readiness probe
      description: Verifies PostgreSQL and Redis are reachable.
      security: []
      responses:
        "200":
          description: All dependencies reachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Readiness"
        "503":
          description: One or more dependencies unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Readiness"

  /api/v1/auth/login:
    post:
      tags: [Auth]
      operationId: authLogin
      summary: Login with email and password
      description: Verifies credentials and issues an access token; sets the refresh token as an `inventra_refresh` HttpOnly cookie.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Tokens issued
          headers:
            Set-Cookie:
              schema: { type: string }
              description: Sets the `inventra_refresh` HttpOnly cookie.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Invalid email or password
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: User is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /api/v1/auth/refresh:
    post:
      tags: [Auth]
      operationId: authRefresh
      summary: Rotate refresh token for a new token pair
      description: >
        Rotates the session using the `inventra_refresh` HttpOnly cookie set at
        login. No request body. Responds 401 if the cookie is missing or invalid.
      security: []
      responses:
        "200":
          description: New token pair issued; the old refresh token is invalidated
          headers:
            Set-Cookie:
              schema: { type: string }
              description: Sets the `inventra_refresh` HttpOnly cookie.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "401":
          description: Invalid or expired refresh token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /api/v1/auth/google:
    get:
      tags: [Auth]
      operationId: authGoogleStart
      summary: Start Google sign-in
      description: Redirects (302) to Google's consent screen. No body.
      security: []
      responses:
        "302":
          description: Redirect to the Google consent screen.
          headers:
            Location:
              schema: { type: string }

  /api/v1/auth/google/callback:
    get:
      tags: [Auth]
      operationId: authGoogleCallback
      summary: Google sign-in callback
      description: >
        Validates state, exchanges the code, verifies the ID token, links the
        account (link-only), sets the `inventra_refresh` HttpOnly cookie, and
        redirects (302) to the SPA at `/login?oauth=success` (or `?oauth=error&reason=...`).
      security: []
      parameters:
        - { in: query, name: code, schema: { type: string } }
        - { in: query, name: state, schema: { type: string } }
        - { in: query, name: error, schema: { type: string } }
      responses:
        "302":
          description: Redirect back to the SPA.
          headers:
            Location:
              schema: { type: string }
            Set-Cookie:
              schema: { type: string }
              description: On success, sets the inventra_refresh HttpOnly cookie.

  /api/v1/auth/logout:
    post:
      tags: [Auth]
      operationId: authLogout
      summary: Revoke the current access token and the refresh cookie
      description: Denylists the current access token until expiry and clears the `inventra_refresh` HttpOnly cookie. No request body.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Logged out
          headers:
            Set-Cookie:
              schema: { type: string }
              description: Clears the `inventra_refresh` HttpOnly cookie.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    examples: ["logged_out"]
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/me:
    get:
      tags: [Auth]
      operationId: authMe
      summary: Current authenticated user
      description: Returns the profile of the user identified by the access token.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /api/v1/auth/permissions:
    get:
      tags: [Auth]
      operationId: authPermissions
      summary: Effective RBAC permissions of the caller
      description: Returns the action permission keys granted to the caller's role.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Permission keys
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/scope/{module}:
    get:
      tags: [Auth]
      operationId: authScope
      summary: Effective data scope of the caller for a module
      description: |
        Resolves the caller's data-access scope (`global`, `office_subtree`,
        `office`, or `own`) for the given module, including the office subtree
        when applicable.
      security:
        - bearerJWT: []
      parameters:
        - name: module
          in: path
          required: true
          description: Module key (e.g. `asset`, `requests`), or `*` for the default.
          schema:
            type: string
      responses:
        "200":
          description: Effective scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Scope"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users:
    get:
      tags: [Users]
      operationId: listUsers
      summary: List users
      description: Requires the `user.manage` permission (Superadmin by default).
      security:
        - bearerJWT: []
      parameters:
        - name: search
          in: query
          schema: { type: string }
          description: Filter by name or email (case-insensitive).
        - name: limit
          in: query
          schema: { type: integer, default: 20, minimum: 1, maximum: 100 }
        - name: offset
          in: query
          schema: { type: integer, default: 0, minimum: 0 }
      responses:
        "200":
          description: A page of users
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserList"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Users]
      operationId: createUser
      summary: Create a user
      description: Requires the `user.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: Created user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Email already in use
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/users/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [Users]
      operationId: getUser
      summary: Get a user by id
      description: Requires the `user.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/User" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Users]
      operationId: updateUser
      summary: Update a user (full replace of mutable fields)
      description: Replaces name, role, status, office, and employee. Requires the `user.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Users]
      operationId: deleteUser
      summary: Soft-delete a user
      description: Marks the user deleted (soft delete). Requires the `user.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/categories/tree:
    get:
      tags: [Master Data]
      operationId: listCategoryTree
      summary: Category tree (full)
      description: Returns the full non-deleted category set, unpaginated, for client-side tree building. Readable by any authenticated user.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Full category list
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Category"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/categories:
    get:
      tags: [Master Data]
      operationId: listCategories
      summary: List asset categories
      description: Readable by any authenticated user.
      security:
        - bearerJWT: []
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
      responses:
        "200":
          description: A page of categories
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CategoryList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Master Data]
      operationId: createCategory
      summary: Create an asset category
      description: Requires the `masterdata.global.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CategoryRequest" }
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Category" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Duplicate code
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/categories/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Master Data]
      operationId: getCategory
      summary: Get a category by id
      description: Readable by any authenticated user.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The category
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Category" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Master Data]
      operationId: updateCategory
      summary: Update a category (full replace)
      description: Requires the `masterdata.global.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CategoryRequest" }
      responses:
        "200":
          description: Updated category
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Category" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Master Data]
      operationId: deleteCategory
      summary: Soft-delete a category
      description: Requires the `masterdata.global.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/brands:
    get:
      tags: [Master Data]
      operationId: listBrands
      summary: List brands (representative reference resource)
      description: Readable by any authenticated user. See the Master Data tag for the full list of resources sharing this contract.
      security: [{ bearerJWT: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
      responses:
        "200":
          description: A page of records
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RefList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Master Data]
      operationId: createBrand
      summary: Create a brand
      description: Requires the `masterdata.global.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RefRequest" }
      responses:
        "201":
          description: Created record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RefItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Duplicate unique value
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/brands/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Master Data]
      operationId: getBrand
      summary: Get a brand by id
      description: Readable by any authenticated user.
      security: [{ bearerJWT: [] }]
      responses:
        "200":
          description: The record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RefItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Master Data]
      operationId: updateBrand
      summary: Update a brand (full replace)
      description: Requires the `masterdata.global.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RefRequest" }
      responses:
        "200":
          description: Updated record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RefItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Master Data]
      operationId: deleteBrand
      summary: Soft-delete a brand
      description: Requires the `masterdata.global.manage` permission.
      security: [{ bearerJWT: [] }]
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/offices:
    get:
      tags: [Master Data]
      operationId: listOffices
      summary: List offices (data-scoped)
      description: |
        Returns offices within the caller's **data scope**: `global` sees all;
        `office_subtree` sees the caller's office and its descendants; etc.
      security: [{ bearerJWT: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
      responses:
        "200":
          description: A page of offices in scope
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OfficeList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Master Data]
      operationId: createOffice
      summary: Create an office
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OfficeRequest" }
      responses:
        "201":
          description: Created office
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Office" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/offices/map:
    get:
      tags: [Master Data]
      operationId: listOfficesMap
      summary: Office map
      description: |
        Returns a geo-enriched list of active offices within the caller's **data scope**,
        with resolved office-type name, province/city names, and per-office asset count.
        No pagination — returns all in-scope offices. Used by the Peta Lokasi screen.
      security: [{ bearerJWT: [] }]
      responses:
        "200":
          description: List of in-scope offices for the map
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/OfficeMapItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/offices/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Master Data]
      operationId: getOffice
      summary: Get an office by id
      description: Readable by any authenticated user.
      security: [{ bearerJWT: [] }]
      responses:
        "200": { description: The office, content: { application/json: { schema: { $ref: "#/components/schemas/Office" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    put:
      tags: [Master Data]
      operationId: updateOffice
      summary: Update an office (full replace)
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/OfficeRequest" } } }
      responses:
        "200": { description: Updated office, content: { application/json: { schema: { $ref: "#/components/schemas/Office" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    delete:
      tags: [Master Data]
      operationId: deleteOffice
      summary: Soft-delete an office
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  /api/v1/employees:
    get:
      tags: [Master Data]
      operationId: listEmployees
      summary: List employees (data-scoped)
      description: Returns employees whose office is within the caller's data scope.
      security: [{ bearerJWT: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
      responses:
        "200":
          description: A page of employees in scope
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Master Data]
      operationId: createEmployee
      summary: Create an employee
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EmployeeRequest" } } }
      responses:
        "201": { description: Created employee, content: { application/json: { schema: { $ref: "#/components/schemas/Employee" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { description: Duplicate code, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  /api/v1/employees/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Master Data]
      operationId: getEmployee
      summary: Get an employee by id
      description: Readable by any authenticated user.
      security: [{ bearerJWT: [] }]
      responses:
        "200": { description: The employee, content: { application/json: { schema: { $ref: "#/components/schemas/Employee" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    put:
      tags: [Master Data]
      operationId: updateEmployee
      summary: Update an employee (full replace)
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EmployeeRequest" } } }
      responses:
        "200": { description: Updated employee, content: { application/json: { schema: { $ref: "#/components/schemas/Employee" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    delete:
      tags: [Master Data]
      operationId: deleteEmployee
      summary: Soft-delete an employee
      description: Requires the `masterdata.office.manage` permission.
      security: [{ bearerJWT: [] }]
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  /api/v1/assets:
    get:
      tags: [Assets]
      operationId: listAssets
      summary: List assets (data-scoped)
      description: |
        Returns assets within the caller's data scope. Requires the `asset.view`
        permission. Sensitive financial fields (purchase_cost, book_value,
        accumulated_depreciation) may be masked by field-permission policies.
      security:
        - bearerJWT: []
      parameters:
        - { name: search, in: query, schema: { type: string }, description: Filter by name or asset_tag (case-insensitive). }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
        - name: category_id
          in: query
          schema: { type: string, format: uuid }
          description: Filter by category.
        - name: office_id
          in: query
          schema: { type: string, format: uuid }
          description: Filter by office.
        - name: status
          in: query
          schema:
            type: string
            enum: [available, assigned, under_maintenance, in_transfer, retired, disposed, lost]
          description: Filter by asset status.
        - name: asset_class
          in: query
          schema:
            type: string
            enum: [tangible, intangible]
          description: Filter by asset class.
      responses:
        "200":
          description: A page of assets
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssetList" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/assets/by-tag/{tag}:
    parameters:
      - name: tag
        in: path
        required: true
        schema: { type: string }
        description: Asset tag (barcode / QR value) to look up.
    get:
      tags: [Assets]
      operationId: getAssetByTag
      summary: Scan-lookup — get asset by tag
      description: |
        Resolves an asset by its `asset_tag` value (scanned from a barcode or QR code).
        Requires the `asset.view` permission. Out-of-scope assets return 404 (not 403)
        to avoid tag enumeration.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The asset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Asset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: Tag not found or out of caller's scope
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/labels:
    post:
      tags: [Assets]
      operationId: generateLabels
      summary: Generate a label PDF for one or more assets
      description: |
        Renders a printable label PDF for the supplied assets (by id or tag).
        Provide `asset_ids` OR `tags`; if both are given, `asset_ids` takes precedence.
        Requires the `asset.view` permission.
        Returns 422 when none of the requested assets fall within the caller's scope.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LabelRequest" }
      responses:
        "200":
          description: Label PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422":
          description: No in-scope assets matched the request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Assets]
      operationId: getAsset
      summary: Get an asset by id
      description: Requires the `asset.view` permission. The asset must be within the caller's data scope.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The asset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Asset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Assets]
      operationId: updateAsset
      summary: Update non-sensitive asset attributes
      description: |
        Replaces the mutable non-financial attributes of an asset.
        Requires the `asset.manage` permission; the asset must be within the caller's data scope.
        `purchase_cost` and `asset_class` are not updated here — they are changed via dedicated operations.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssetUpdate" }
      responses:
        "200":
          description: Updated asset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Asset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/barcode:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Assets]
      operationId: getAssetBarcode
      summary: Get barcode/QR image for an asset
      description: |
        Returns a PNG image of the asset's barcode (Code-128) or QR code.
        Requires the `asset.view` permission. Out-of-scope assets return 403.
      security:
        - bearerJWT: []
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum: [code128, qr]
            default: code128
          description: Barcode symbology to render.
      responses:
        "200":
          description: PNG barcode image
          content:
            image/png:
              schema:
                type: string
                format: binary
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Asset not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/attachments:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
    post:
      tags: [Assets]
      operationId: uploadAttachment
      summary: Upload an attachment to an asset
      description: |
        Attaches a file (photo or document) to the given asset.
        Requires the `asset.manage` permission.
        Returns 413 if the file exceeds the server size limit; 415 if the MIME type is not accepted.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: The file to upload.
      responses:
        "201":
          description: Attachment created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Attachment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "413":
          description: File too large
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "415":
          description: Unsupported media type
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    get:
      tags: [Assets]
      operationId: listAttachments
      summary: List attachments for an asset
      description: Returns all non-deleted attachments for the given asset. Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Attachment list
          content:
            application/json:
              schema:
                type: object
                required: [data, total]
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Attachment" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/assets/{id}/attachments/{aid}/content:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid }, description: Attachment id }
    get:
      tags: [Assets]
      operationId: getAttachmentContent
      summary: Download the raw content of an attachment
      description: Streams the original file bytes. Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: File contents
          content:
            image/jpeg:
              schema: { type: string, format: binary }
            image/png:
              schema: { type: string, format: binary }
            image/webp:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
            application/octet-stream:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Attachment not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/attachments/{aid}/thumbnail:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid }, description: Attachment id }
    get:
      tags: [Assets]
      operationId: getAttachmentThumbnail
      summary: Download the JPEG thumbnail of an attachment
      description: |
        Streams the thumbnail image (JPEG). Only available when `has_thumbnail` is true on the
        attachment. Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Thumbnail image
          content:
            image/jpeg:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Attachment or thumbnail not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/attachments/{aid}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid }, description: Attachment id }
    delete:
      tags: [Assets]
      operationId: deleteAttachment
      summary: Delete an attachment
      description: Soft-deletes the attachment record and removes the backing object from storage. Requires the `asset.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Attachment not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/documents:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
    get:
      tags: [Assets]
      operationId: listDocuments
      summary: List documents for an asset
      description: Returns all non-deleted documents (BAST and other) for the given asset. Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Document list
          content:
            application/json:
              schema:
                type: object
                required: [data, total]
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/AssetDocument" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Assets]
      operationId: createDocument
      summary: Create a document record for an asset
      description: |
        Creates a document metadata record (BAST or other). A file may be attached separately
        via `PUT /assets/{id}/documents/{docId}/file`. Requires the `asset.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DocumentCreateRequest" }
      responses:
        "201":
          description: Document created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssetDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/assets/{id}/documents/{docId}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
      - { name: docId, in: path, required: true, schema: { type: string, format: uuid }, description: Document id }
    get:
      tags: [Assets]
      operationId: getDocument
      summary: Get a document record
      description: Returns a single document belonging to the given asset. Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The document
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssetDocument" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Document not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Assets]
      operationId: updateDocument
      summary: Update document metadata
      description: Edits the metadata fields of a document. Does not affect any attached file. Requires the `asset.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DocumentUpdateRequest" }
      responses:
        "200":
          description: Updated document
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssetDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Document not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Assets]
      operationId: deleteDocument
      summary: Delete a document record
      description: Soft-deletes the document record and removes any backing file from storage. Requires the `asset.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Document not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/assets/{id}/documents/{docId}/file:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Asset id }
      - { name: docId, in: path, required: true, schema: { type: string, format: uuid }, description: Document id }
    put:
      tags: [Assets]
      operationId: uploadDocumentFile
      summary: Attach a file to a document record
      description: |
        Uploads (or replaces) the file associated with the document. Accepted MIME types mirror
        the asset-attachment endpoint (image/jpeg, image/png, image/webp, application/pdf).
        Returns 413 if the file exceeds the server size limit; 415 if
        the MIME type is not accepted. Requires the `asset.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: The file to attach.
      responses:
        "200":
          description: Document with file attached
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssetDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Document not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "413":
          description: File too large
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "415":
          description: Unsupported media type
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    get:
      tags: [Assets]
      operationId: downloadDocumentFile
      summary: Download the file attached to a document
      description: |
        Streams the raw file bytes with `Content-Disposition: attachment` and security headers
        (`X-Content-Type-Options: nosniff`, `Content-Security-Policy: sandbox`).
        Returns 404 when no file has been attached (`has_file` is false). Requires the `asset.view` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: File contents
          content:
            image/jpeg:
              schema: { type: string, format: binary }
            image/png:
              schema: { type: string, format: binary }
            image/webp:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
            application/octet-stream:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Document or file not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/requests:
    post:
      tags: [Requests]
      operationId: submitRequest
      summary: Submit an approval request
      description: Creates a new approval request and routes it to the first eligible approver. Requires the `request.create` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitRequest" }
      responses:
        "201":
          description: Created request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Request" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422":
          description: No matching approval threshold configured
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    get:
      tags: [Requests]
      operationId: listRequests
      summary: List approval requests (data-scoped)
      description: Returns requests within the caller's data scope. No special permission beyond authentication required for listing.
      security:
        - bearerJWT: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, approved, rejected, cancelled]
          description: Filter by request status.
        - name: type
          in: query
          schema:
            type: string
            enum: [asset_create, asset_disposal, asset_transfer, assignment, maintenance, valuation_exclusion]
          description: Filter by request type.
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
        - { name: offset, in: query, schema: { type: integer, default: 0, minimum: 0 } }
      responses:
        "200":
          description: A page of requests
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RequestList" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/requests/inbox:
    get:
      tags: [Requests]
      operationId: listRequestsInbox
      summary: Pending requests awaiting the caller's decision
      description: Returns requests currently at a step the caller is eligible to approve or reject. Requires the `request.decide` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Requests in inbox
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RequestInbox" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/requests/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Requests]
      operationId: getRequest
      summary: Get a request by id (with approval steps)
      description: Returns the request and its full approval step history.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The request with steps
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RequestWithSteps" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/requests/{id}/approve:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [Requests]
      operationId: approveRequest
      summary: Approve a pending request
      description: Moves the request to the next approval step or marks it approved when all steps pass. Requires the `request.decide` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DecideRequest" }
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Request" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: Request is not in a state that allows approval
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/requests/{id}/reject:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [Requests]
      operationId: rejectRequest
      summary: Reject a pending request
      description: Marks the request as rejected at the current step. Requires the `request.decide` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DecideRequest" }
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Request" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: Request is not in a state that allows rejection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/requests/{id}/cancel:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [Requests]
      operationId: cancelRequest
      summary: Cancel a request (maker only)
      description: Cancels the request. Only the original maker may cancel, and only while the request is still pending.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Cancelled request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Request" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: Request cannot be cancelled in its current state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/approval-thresholds:
    get:
      tags: [Approval Thresholds]
      operationId: listThresholds
      summary: List approval thresholds
      description: Returns all configured approval thresholds. Requires the `approval.config.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: All thresholds
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ThresholdList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Approval Thresholds]
      operationId: createThreshold
      summary: Create an approval threshold
      description: Requires the `approval.config.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ThresholdRequest" }
      responses:
        "201":
          description: Created threshold
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Threshold" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Duplicate threshold configuration
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/approval-thresholds/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [Approval Thresholds]
      operationId: updateThreshold
      summary: Update an approval threshold
      description: |
        Replaces mutable threshold fields (`amount_from`, `amount_to`, `required_level`,
        `step_order`, `is_active`). `request_type` is immutable after creation.
        Requires the `approval.config.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ThresholdRequest" }
      responses:
        "200":
          description: Updated threshold
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Threshold" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Approval Thresholds]
      operationId: deleteThreshold
      summary: Delete an approval threshold
      description: Requires the `approval.config.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/authz/catalog:
    get:
      tags: [Authorization Admin]
      operationId: getAuthzCatalog
      summary: Get the canonical authorization catalog
      description: |
        Returns the complete set of assignable permission keys (grouped for the admin UI),
        valid data-scope levels, and known scope-module strings. Used to drive the
        Peran & RBAC / Data Scope / Field Permission admin screens.
        Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The authorization catalog
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthzCatalog"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/authz/roles:
    get:
      tags: [Authorization Admin]
      operationId: listRoles
      summary: List roles
      description: Returns all non-deleted roles. Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: All roles
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleList"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Authorization Admin]
      operationId: createRole
      summary: Create a role
      description: Creates a new non-system role. Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RoleCreateRequest"
      responses:
        "201":
          description: Created role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Role" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Duplicate role code
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/authz/roles/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Authorization Admin]
      operationId: getRole
      summary: Get a role by id
      description: Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: The role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Role" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Authorization Admin]
      operationId: updateRole
      summary: Update a role (full replace of mutable fields)
      description: |
        Replaces `code`, `name`, and `description`. System roles (`is_system: true`)
        cannot be updated and return 409. Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RoleUpdateRequest"
      responses:
        "200":
          description: Updated role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Role" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: Duplicate code or attempt to mutate a system role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Authorization Admin]
      operationId: deleteRole
      summary: Soft-delete a role
      description: |
        Marks the role deleted. System roles (`is_system: true`) and roles still
        assigned to any user cannot be deleted (both return 409).
        Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "204": { description: Deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: System role or role is still assigned to one or more users
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/authz/roles/{id}/permissions:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Authorization Admin]
      operationId: getRolePermissions
      summary: Get the permission keys assigned to a role
      description: Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Permission set for this role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PermissionSet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Authorization Admin]
      operationId: setRolePermissions
      summary: Replace the full permission set of a role
      description: |
        Atomically replaces all `role_permissions` rows for this role with the
        supplied list (replace-set semantics). Deduplicates the input. All keys
        must be in the canonical catalog; unknown keys return 400.
        Invalidates the Redis RBAC cache for this role.
        Requires the `role.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PermissionSet"
      responses:
        "200":
          description: Updated permission set
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PermissionSet" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/authz/roles/{id}/scope:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Authorization Admin]
      operationId: getRoleScope
      summary: Get the data-scope policies for a role
      description: Requires the `scope.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Scope-policy set for this role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ScopePolicySet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Authorization Admin]
      operationId: setRoleScope
      summary: Replace the full data-scope policy set of a role
      description: |
        Atomically replaces all `data_scope_policies` rows for this role with the
        supplied list (replace-set semantics). `scope_level` must be one of the
        values returned by `GET /authz/catalog` (`scope_levels`); `module` must be
        one of the `scope_modules` values or `*` (the default catch-all).
        Invalidates the Redis scope cache for this role.
        Requires the `scope.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScopePolicySet"
      responses:
        "200":
          description: Updated scope-policy set
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ScopePolicySet" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/authz/roles/{id}/fields:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [Authorization Admin]
      operationId: getRoleFields
      summary: Get the field-permission entries for a role
      description: Requires the `fieldperm.manage` permission.
      security:
        - bearerJWT: []
      responses:
        "200":
          description: Field-permission set for this role
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FieldPermissionSet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [Authorization Admin]
      operationId: setRoleFields
      summary: Replace the full field-permission set of a role
      description: |
        Atomically replaces all `field_permissions` rows for this role with the
        supplied list (replace-set semantics). Each entry identifies an `(entity, field)`
        pair with `can_view` and `can_edit` booleans. The default policy is allow-all —
        only entries that restrict access need to be included, but supplying a full
        explicit set is valid and idiomatic.
        Invalidates the Redis field-permission cache for this role.
        Requires the `fieldperm.manage` permission.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FieldPermissionSet"
      responses:
        "200":
          description: Updated field-permission set
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FieldPermissionSet" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Role not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/audit:
    get:
      tags: [Audit]
      operationId: listAuditLogs
      summary: List audit-trail entries
      description: |
        Read-only audit log, newest first. Requires the `audit.view` permission.
        Results are office-scoped to the caller (global-scope callers see all rows,
        including global/no-office actions).
      security:
        - bearerJWT: []
      parameters:
        - name: search
          in: query
          schema: { type: string }
          description: Filter by entity type or entity id (case-insensitive).
        - name: actor_id
          in: query
          schema: { type: string, format: uuid }
          description: Filter by the acting user.
        - name: entity_type
          in: query
          schema: { type: string }
          description: Filter by entity type (e.g. `offices`, `users`, `brands`).
        - name: action
          in: query
          schema: { type: string, enum: [create, update, delete] }
        - name: from
          in: query
          schema: { type: string, format: date-time }
          description: Only entries at or after this time (RFC3339).
        - name: to
          in: query
          schema: { type: string, format: date-time }
          description: Only entries at or before this time (RFC3339).
        - name: limit
          in: query
          schema: { type: integer, default: 20, minimum: 1, maximum: 100 }
        - name: offset
          in: query
          schema: { type: integer, default: 0, minimum: 0 }
      responses:
        "200":
          description: A page of audit entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditList"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

components:
  securitySchemes:
    bearerJWT:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    BadRequest:
      description: Malformed request body
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Missing, invalid, or revoked token
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Authenticated but lacking the required permission
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    TooManyRequests:
      description: Rate limit exceeded.
      headers:
        Retry-After:
          schema: { type: integer }
          description: Seconds until the client may retry.
        RateLimit-Limit:
          schema: { type: integer }
        RateLimit-Remaining:
          schema: { type: integer }
        RateLimit-Reset:
          schema: { type: integer }
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
          description: Human-readable error message
      examples:
        - error: invalid email or password

    Health:
      type: object
      required: [status, service, env]
      properties:
        status:
          type: string
          examples: ["ok"]
        service:
          type: string
          examples: ["inventra-api"]
        env:
          type: string
          examples: ["development"]

    Readiness:
      type: object
      required: [status, checks]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        checks:
          type: object
          properties:
            postgres:
              type: string
              description: '"ok" or an error message'
            redis:
              type: string
              description: '"ok" or an error message'

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          examples: ["admin@inventra.local"]
        password:
          type: string
          format: password
          minLength: 1
          examples: ["admin12345"]

    TokenResponse:
      type: object
      required: [access_token, token_type, expires_in]
      properties:
        access_token:
          type: string
        token_type:
          type: string
          examples: ["Bearer"]
        expires_in:
          type: integer
          description: Access-token lifetime in seconds
          examples: [900]

    User:
      type: object
      required: [id, name, email, role_id, status, google_linked]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        role_id:
          type: string
          format: uuid
        office_id:
          type: [string, "null"]
          format: uuid
        employee_id:
          type: [string, "null"]
          format: uuid
        status:
          type: string
          enum: [active, inactive, suspended]
        avatar_url:
          type: [string, "null"]
        google_linked:
          type: boolean
        created_at:
          type: [string, "null"]
          format: date-time
        updated_at:
          type: [string, "null"]
          format: date-time

    CreateUserRequest:
      type: object
      required: [name, email, role_id]
      properties:
        name: { type: string }
        email: { type: string, format: email }
        password:
          type: string
          format: password
          description: Optional; when omitted the user can only sign in via Google.
        role_id: { type: string, format: uuid }
        office_id: { type: [string, "null"], format: uuid }
        employee_id: { type: [string, "null"], format: uuid }

    UpdateUserRequest:
      type: object
      required: [name, role_id, status]
      properties:
        name: { type: string }
        role_id: { type: string, format: uuid }
        status: { type: string, enum: [active, inactive, suspended] }
        office_id: { type: [string, "null"], format: uuid }
        employee_id: { type: [string, "null"], format: uuid }

    UserList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/User" }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    AuditActor:
      type: object
      required: [id]
      properties:
        id: { type: string, format: uuid }
        name: { type: [string, "null"] }
        email: { type: [string, "null"] }

    AuditLog:
      type: object
      required: [id, entity_type, entity_id, action, created_at]
      properties:
        id: { type: string, format: uuid }
        actor: { oneOf: [{ $ref: "#/components/schemas/AuditActor" }, { type: "null" }] }
        entity_type: { type: string }
        entity_id: { type: string, format: uuid }
        action: { type: string, enum: [create, update, delete] }
        changes:
          description: "Changed fields keyed by name, each with optional before/after values (before omitted on create, after omitted on delete)."
          type: [object, "null"]
          additionalProperties: true
        ip: { type: [string, "null"] }
        office_id: { type: [string, "null"], format: uuid }
        created_at: { type: string, format: date-time }

    AuditList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/AuditLog" }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    Category:
      type: object
      required: [id, name, is_active]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        code: { type: [string, "null"] }
        parent_id: { type: [string, "null"], format: uuid }
        default_depreciation_method:
          type: [string, "null"]
          description: One of `straight_line` or `declining_balance` (nullable).
        default_useful_life_months: { type: [integer, "null"] }
        default_salvage_rate:
          type: [string, "null"]
          description: Decimal string (e.g. "0.1000" = 10%).
        asset_class:
          type: string
          description: "`tangible` or `intangible` (PRD v1.1)."
        default_fiscal_group:
          type: [string, "null"]
          description: Fiscal/tax depreciation group (PMK 72/2023) — `kelompok_1`..`kelompok_4`, `bangunan_permanen`, `bangunan_non_permanen`, `non_susut`.
        default_fiscal_life_months: { type: [integer, "null"] }
        gl_account_code: { type: [string, "null"] }
        capitalization_threshold:
          type: [string, "null"]
          description: Decimal string (IDR). Below this, an acquisition is expensed, not capitalized.
        is_active: { type: boolean }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }

    CategoryRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        code: { type: [string, "null"] }
        parent_id: { type: [string, "null"], format: uuid }
        default_depreciation_method:
          type: [string, "null"]
          description: One of `straight_line` or `declining_balance` (nullable).
        default_useful_life_months: { type: [integer, "null"] }
        default_salvage_rate: { type: [string, "null"] }
        asset_class:
          type: [string, "null"]
          description: "`tangible` or `intangible`; defaults to `tangible` when omitted."
        default_fiscal_group:
          type: [string, "null"]
          description: "`kelompok_1`..`kelompok_4`, `bangunan_permanen`, `bangunan_non_permanen`, or `non_susut`."
        default_fiscal_life_months: { type: [integer, "null"] }
        gl_account_code: { type: [string, "null"] }
        capitalization_threshold: { type: [string, "null"] }
        is_active: { type: boolean, default: true }

    CategoryList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/Category" }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    RefItem:
      type: object
      description: A reference master-data record. Common fields shown; resources add their own (e.g. code, symbol, is_active, province_id, brand_id).
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        tier:
          type: [string, "null"]
          enum: [pusat, wilayah, office]
          description: Office tier (drives the office-map pin category). Applies to `office-types` only.
        is_active: { type: boolean }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }
      additionalProperties: true

    RefRequest:
      type: object
      description: Writable fields of a reference record (varies per resource; `name` is always required).
      required: [name]
      properties:
        name: { type: string }
        tier:
          type: [string, "null"]
          enum: [pusat, wilayah, office]
          description: Office tier (drives the office-map pin category). Applies to `office-types` only.
        is_active: { type: boolean, default: true }
      additionalProperties: true

    RefList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/RefItem" }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    Office:
      type: object
      required: [id, office_type_id, name, code, is_active]
      properties:
        id: { type: string, format: uuid }
        parent_id: { type: [string, "null"], format: uuid }
        office_type_id: { type: string, format: uuid }
        province_id: { type: [string, "null"], format: uuid }
        city_id: { type: [string, "null"], format: uuid }
        name: { type: string }
        code: { type: string }
        address: { type: [string, "null"] }
        is_active: { type: boolean }
        latitude: { type: [number, "null"], minimum: -90, maximum: 90 }
        longitude: { type: [number, "null"], minimum: -180, maximum: 180 }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }

    OfficeRequest:
      type: object
      required: [office_type_id, name, code]
      properties:
        parent_id: { type: [string, "null"], format: uuid }
        office_type_id: { type: string, format: uuid }
        province_id: { type: [string, "null"], format: uuid }
        city_id: { type: [string, "null"], format: uuid }
        name: { type: string }
        code: { type: string }
        address: { type: [string, "null"] }
        is_active: { type: boolean, default: true }
        latitude: { type: [number, "null"], minimum: -90, maximum: 90 }
        longitude: { type: [number, "null"], minimum: -180, maximum: 180 }

    OfficeMapItem:
      type: object
      required: [id, name, code, asset_count]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        code: { type: string }
        office_type_name: { type: [string, "null"] }
        tier:
          type: [string, "null"]
          enum: [pusat, wilayah, office, office_subtree]
          nullable: true
        province_name: { type: [string, "null"] }
        city_name: { type: [string, "null"] }
        address: { type: [string, "null"] }
        asset_count: { type: integer, minimum: 0 }
        latitude: { type: [number, "null"], minimum: -90, maximum: 90 }
        longitude: { type: [number, "null"], minimum: -180, maximum: 180 }

    OfficeList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Office" } }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    Employee:
      type: object
      required: [id, code, name, office_id, status]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        email: { type: [string, "null"], format: email }
        phone: { type: [string, "null"] }
        avatar_key: { type: [string, "null"] }
        department_id: { type: [string, "null"], format: uuid }
        position_id: { type: [string, "null"], format: uuid }
        office_id: { type: string, format: uuid }
        status: { type: string, enum: [active, inactive, suspended] }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }

    EmployeeRequest:
      type: object
      required: [code, name, office_id]
      properties:
        code: { type: string }
        name: { type: string }
        email: { type: [string, "null"], format: email }
        phone: { type: [string, "null"] }
        avatar_key: { type: [string, "null"] }
        department_id: { type: [string, "null"], format: uuid }
        position_id: { type: [string, "null"], format: uuid }
        office_id: { type: string, format: uuid }
        status: { type: string, enum: [active, inactive, suspended], default: active }

    EmployeeList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Employee" } }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    PermissionsResponse:
      type: object
      required: [permissions]
      properties:
        permissions:
          type: array
          items:
            type: string
          examples:
            - ["asset.read", "request.create", "report.view"]

    Scope:
      type: object
      required: [level]
      properties:
        level:
          type: string
          enum: [global, office_subtree, office, own]
        office_ids:
          type: array
          description: Offices in scope (for office / office_subtree levels).
          items:
            type: string
            format: uuid

    Asset:
      type: object
      description: |
        A fixed asset record. Sensitive financial fields (`purchase_cost`, `book_value`,
        `accumulated_depreciation`) are included in the schema but may be omitted at
        runtime by field-permission masking depending on the caller's role.
      required: [id, asset_tag, name, category_id, office_id, status, asset_class]
      properties:
        id: { type: string, format: uuid }
        asset_tag: { type: string }
        name: { type: string }
        category_id: { type: string, format: uuid }
        office_id: { type: string, format: uuid }
        brand_id: { type: [string, "null"], format: uuid }
        model_id: { type: [string, "null"], format: uuid }
        room_id: { type: [string, "null"], format: uuid }
        unit_id: { type: [string, "null"], format: uuid }
        vendor_id: { type: [string, "null"], format: uuid }
        current_holder_employee_id: { type: [string, "null"], format: uuid }
        created_by_id: { type: [string, "null"], format: uuid }
        status:
          type: string
          enum: [available, assigned, under_maintenance, in_transfer, retired, disposed, lost]
        asset_class:
          type: string
          enum: [tangible, intangible]
        serial_number: { type: [string, "null"] }
        purchase_date: { type: [string, "null"], format: date }
        purchase_cost:
          type: [string, "null"]
          description: Decimal string (IDR). Subject to field-permission masking.
        book_value:
          type: [string, "null"]
          description: Decimal string (IDR). Subject to field-permission masking.
        accumulated_depreciation:
          type: [string, "null"]
          description: Decimal string (IDR). Subject to field-permission masking.
        salvage_value: { type: [string, "null"] }
        impairment_loss: { type: [string, "null"] }
        po_number: { type: [string, "null"] }
        funding_source: { type: [string, "null"] }
        warranty_expiry: { type: [string, "null"], format: date }
        capitalized: { type: [boolean, "null"] }
        depreciation_method: { type: [string, "null"] }
        useful_life_months: { type: [integer, "null"] }
        fiscal_group: { type: [string, "null"] }
        fiscal_life_months: { type: [integer, "null"] }
        acquisition_bast_no: { type: [string, "null"] }
        excluded_from_valuation: { type: [boolean, "null"] }
        valuation_exclusion_reason: { type: [string, "null"] }
        notes: { type: [string, "null"] }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }

    AssetUpdate:
      type: object
      description: Writable non-financial asset attributes. `purchase_cost` and `asset_class` are changed via dedicated operations.
      required: [name, category_id]
      properties:
        name: { type: string }
        category_id: { type: string, format: uuid }
        brand_id: { type: [string, "null"], format: uuid }
        model_id: { type: [string, "null"], format: uuid }
        room_id: { type: [string, "null"], format: uuid }
        unit_id: { type: [string, "null"], format: uuid }
        vendor_id: { type: [string, "null"], format: uuid }
        serial_number: { type: [string, "null"] }
        po_number: { type: [string, "null"] }
        funding_source: { type: [string, "null"] }
        purchase_date: { type: [string, "null"], format: date, description: "YYYY-MM-DD" }
        warranty_expiry: { type: [string, "null"], format: date, description: "YYYY-MM-DD" }
        notes: { type: [string, "null"] }

    AssetList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Asset" } }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    SubmitRequest:
      type: object
      description: Body for POST /requests.
      required: [type, amount, office_id]
      properties:
        type:
          type: string
          enum: [asset_create, asset_disposal, valuation_exclusion]
        amount:
          type: string
          description: Decimal string (IDR). Determines which threshold steps apply.
        office_id:
          type: string
          format: uuid
          description: Office initiating the request.
        target_id:
          type: [string, "null"]
          format: uuid
          description: Optional reference to the target entity (e.g. asset id for disposal).
        payload:
          type: [object, "null"]
          additionalProperties: true
          description: Arbitrary request payload (e.g. asset details for asset_create).
        reason:
          type: [string, "null"]
          description: Optional narrative justification.

    DecideRequest:
      type: object
      description: Optional body for POST /requests/{id}/approve or /reject. The decision is determined by the route; only an optional note is read.
      properties:
        note:
          type: [string, "null"]
          description: Optional decision note.

    Request:
      type: object
      description: An approval request record.
      required: [id, type, status, amount, current_step, requested_by_id, created_at]
      properties:
        id: { type: string, format: uuid }
        type:
          type: string
          enum: [asset_create, asset_disposal, asset_transfer, assignment, maintenance, valuation_exclusion]
        status:
          type: string
          enum: [pending, approved, rejected, cancelled]
        amount:
          type: string
          description: Decimal string (IDR).
        current_step: { type: integer }
        office_id: { type: [string, "null"], format: uuid }
        target_id: { type: [string, "null"], format: uuid }
        target_entity: { type: [string, "null"] }
        reason: { type: [string, "null"] }
        requested_by_id: { type: string, format: uuid }
        decided_by_id: { type: [string, "null"], format: uuid }
        decision_note: { type: [string, "null"] }
        created_at: { type: [string, "null"], format: date-time }

    RequestWithSteps:
      allOf:
        - $ref: "#/components/schemas/Request"
        - type: object
          properties:
            steps:
              type: array
              description: Ordered approval-step history for this request.
              items:
                type: object
                additionalProperties: true

    RequestList:
      type: object
      required: [data, total, limit, offset]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Request" } }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    RequestInbox:
      type: object
      required: [data, total]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Request" } }
        total: { type: integer }

    Threshold:
      type: object
      description: A single approval-threshold rule mapping request type + amount range to a required approver level.
      required: [id, request_type, amount_from, required_level, step_order, is_active]
      properties:
        id: { type: string, format: uuid }
        request_type:
          type: string
          enum: [asset_create, asset_disposal, asset_transfer, assignment, maintenance, valuation_exclusion]
        amount_from:
          type: string
          description: Lower bound (IDR decimal string, inclusive).
        amount_to:
          type: [string, "null"]
          description: Upper bound (IDR decimal string, inclusive). Null means no upper bound.
        required_level:
          type: string
          enum: [office, office_subtree, wilayah, pusat]
        step_order: { type: integer }
        is_active: { type: boolean }
        created_at: { type: [string, "null"], format: date-time }

    ThresholdRequest:
      type: object
      description: Body for POST/PUT /approval-thresholds.
      required: [request_type, amount_from, required_level, step_order]
      properties:
        request_type:
          type: string
          enum: [asset_create, asset_disposal, asset_transfer, assignment, maintenance, valuation_exclusion]
          description: Immutable after creation (ignored on PUT).
        amount_from:
          type: string
          description: Lower bound (IDR decimal string).
        amount_to:
          type: [string, "null"]
          description: Upper bound (IDR decimal string). Omit or null for no upper bound.
        required_level:
          type: string
          enum: [office, office_subtree, wilayah, pusat]
        step_order:
          type: integer
          description: Order of this step within the threshold chain (1-based).
        is_active:
          type: boolean
          default: false

    ThresholdList:
      type: object
      required: [data, total]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Threshold" } }
        total: { type: integer }

    Attachment:
      type: object
      description: |
        An asset attachment (photo or document). Storage-internal fields (`object_key`,
        `thumbnail_key`) are intentionally omitted; use `has_thumbnail` to conditionally
        render thumbnails via `GET /assets/{id}/attachments/{aid}/thumbnail`.
      required: [id, asset_id, kind, original_filename, size_bytes, mime_type, has_thumbnail, created_at]
      properties:
        id: { type: string, format: uuid }
        asset_id: { type: string, format: uuid }
        kind:
          type: string
          enum: [photo, document]
        original_filename: { type: string }
        size_bytes:
          type: integer
          format: int64
        mime_type: { type: string }
        has_thumbnail: { type: boolean }
        created_at: { type: string, format: date-time }

    AssetDocument:
      type: object
      description: |
        A document record attached to an asset (e.g. BAST acquisition, BAST transfer, invoice).
        Storage-internal fields (`object_key`) are intentionally omitted; use `has_file` to
        conditionally offer a download via `GET /assets/{id}/documents/{docId}/file`.
      required: [id, asset_id, doc_type, has_file, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        asset_id: { type: string, format: uuid }
        doc_type:
          type: string
          enum: [bast_acquisition, bast_transfer, bast_disposal, invoice, contract, other]
        doc_no:
          type: string
          nullable: true
        doc_date:
          type: string
          format: date
          nullable: true
        counterparty:
          type: string
          nullable: true
        related_request_id:
          type: string
          format: uuid
          nullable: true
        related_transfer_id:
          type: string
          format: uuid
          nullable: true
        related_disposal_id:
          type: string
          format: uuid
          nullable: true
        has_file: { type: boolean }
        created_by_id:
          type: string
          format: uuid
          nullable: true
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    DocumentCreateRequest:
      type: object
      required: [doc_type]
      properties:
        doc_type:
          type: string
          enum: [bast_acquisition, bast_transfer, bast_disposal, invoice, contract, other]
        doc_no:
          type: string
          nullable: true
        doc_date:
          type: string
          format: date
          nullable: true
        counterparty:
          type: string
          nullable: true
        related_request_id:
          type: string
          format: uuid
          nullable: true

    DocumentUpdateRequest:
      type: object
      required: [doc_type]
      properties:
        doc_type:
          type: string
          enum: [bast_acquisition, bast_transfer, bast_disposal, invoice, contract, other]
        doc_no:
          type: string
          nullable: true
        doc_date:
          type: string
          format: date
          nullable: true
        counterparty:
          type: string
          nullable: true
        related_request_id:
          type: string
          format: uuid
          nullable: true

    LabelRequest:
      type: object
      description: |
        Request body for `POST /assets/labels`. Supply either `asset_ids` or `tags`
        (or both). All dimensional fields are in millimetres. When `size` is a known
        preset the individual `w_mm`/`h_mm` fields are ignored; supply them when using
        a custom size.
      properties:
        asset_ids:
          type: array
          items: { type: string, format: uuid }
          description: Asset UUIDs to include in the label sheet.
        tags:
          type: array
          items: { type: string }
          description: Asset tags (barcode values) to include.
        template:
          type: string
          enum: [btn, generic]
          default: btn
          description: Label template. `btn` uses the Bank BTN house style; `generic` is a plain layout.
        layout:
          type: string
          enum: [roll, sheet]
          default: roll
          description: "`roll` outputs a single-column continuous roll; `sheet` tiles labels in a grid."
        size:
          type: string
          description: Named preset size (e.g. `50x30`). When omitted, supply `w_mm` and `h_mm`.
        w_mm:
          type: number
          format: double
          description: Label width in mm (used when `size` is absent or unknown).
        h_mm:
          type: number
          format: double
          description: Label height in mm (used when `size` is absent or unknown).
        media_w_mm:
          type: number
          format: double
          description: Media (paper/roll) width in mm — required for `sheet` layout.
        columns:
          type: integer
          description: Number of label columns per row (`sheet` layout only).
        mode:
          type: string
          enum: [barcode, qr, both]
          default: barcode
          description: Which symbol to print on each label.
        fields:
          type: object
          description: Optional fields to print on each label alongside the barcode.
          properties:
            name:
              type: boolean
              description: Print the asset name.
            office:
              type: boolean
              description: Print the office name.

    Role:
      type: object
      description: A configurable role in the identity & authorization layer.
      required: [id, code, name, is_system, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        is_system:
          type: boolean
          description: System roles cannot be updated or deleted.
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }

    RoleCreateRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: Unique short identifier for the role (e.g. `branch_manager`).
        name:
          type: string
          description: Human-readable display name.
        description: { type: [string, "null"] }

    RoleUpdateRequest:
      type: object
      required: [name]
      properties:
        code:
          type: string
          description: Unique short identifier. Omit to keep the existing value.
        name:
          type: string
          description: Human-readable display name.
        description: { type: [string, "null"] }

    RoleList:
      type: object
      required: [data, total]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/Role" }
        total: { type: integer }

    PermissionSet:
      type: object
      description: The full set of permission keys assigned to a role.
      required: [permissions]
      properties:
        permissions:
          type: array
          items:
            type: string
          description: |
            Permission keys from the catalog (e.g. `asset.view`, `role.manage`).
            All supplied keys must appear in the catalog returned by `GET /authz/catalog`.

    ScopePolicySet:
      type: object
      description: The full set of data-scope policies for a role.
      required: [policies]
      properties:
        policies:
          type: array
          items:
            $ref: "#/components/schemas/ScopePolicy"

    ScopePolicy:
      type: object
      required: [module, scope_level]
      properties:
        module:
          type: string
          description: |
            Module key (e.g. `assets`, `offices`, `employees`, `requests`) or
            `*` for the default catch-all policy.
        scope_level:
          type: string
          enum: [global, office_subtree, office, own]
          description: Data visibility level for this role and module.

    FieldPermissionSet:
      type: object
      description: The full set of field-permission entries for a role.
      required: [fields]
      properties:
        fields:
          type: array
          items:
            $ref: "#/components/schemas/FieldPermission"

    FieldPermission:
      type: object
      required: [entity, field, can_view, can_edit]
      properties:
        entity:
          type: string
          description: Entity name the restriction applies to (e.g. `assets`).
        field:
          type: string
          description: Field name within the entity (e.g. `purchase_cost`).
        can_view:
          type: boolean
          description: Whether the role may read this field.
        can_edit:
          type: boolean
          description: Whether the role may write this field.

    AuthzCatalog:
      type: object
      description: |
        The canonical authorization catalog. Returned by `GET /authz/catalog` for
        use by admin screens to populate permission pickers, scope-level dropdowns,
        and module selectors.
      required: [permissions, scope_levels, scope_modules]
      properties:
        permissions:
          type: array
          description: Permission groups, each containing assignable permission keys.
          items:
            $ref: "#/components/schemas/PermissionGroup"
        scope_levels:
          type: array
          description: Valid data-scope level values.
          items:
            type: string
          examples:
            - ["global", "office_subtree", "office", "own"]
        scope_modules:
          type: array
          description: Known data-scope module strings (plus `*` for the default policy).
          items:
            type: string
          examples:
            - ["*", "offices", "employees", "assets", "requests"]

    PermissionGroup:
      type: object
      required: [group, items]
      properties:
        group:
          type: string
          description: Display group name (e.g. `Aset`, `Sistem`).
        items:
          type: array
          items:
            $ref: "#/components/schemas/PermissionItem"

    PermissionItem:
      type: object
      required: [key, label]
      properties:
        key:
          type: string
          description: The permission key used in `role_permissions` and `RequirePermission`.
        label:
          type: string
          description: Human-readable label in Indonesian (used by the admin UI).
