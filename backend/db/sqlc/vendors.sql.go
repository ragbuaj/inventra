// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: vendors.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const listVendorsLookup = `-- name: ListVendorsLookup :many

SELECT id, name FROM masterdata.vendors
WHERE deleted_at IS NULL
ORDER BY name
`

type ListVendorsLookupRow struct {
	ID   uuid.UUID `json:"id"`
	Name string    `json:"name"`
}

// Vendor master data (masterdata.vendors). Vendors are managed via the generic
// reference engine; this file holds only bespoke queries needed by other
// modules.
// Flat vendor lookup (id, name) for the asset importer. Vendors are not
// office-scoped, so the full non-deleted set is returned.
func (q *Queries) ListVendorsLookup(ctx context.Context) ([]ListVendorsLookupRow, error) {
	rows, err := q.db.Query(ctx, listVendorsLookup)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListVendorsLookupRow{}
	for rows.Next() {
		var i ListVendorsLookupRow
		if err := rows.Scan(&i.ID, &i.Name); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
